{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci && npm run build",
  "installCommand": "uv pip install -r requirements.txt || pip install -r requirements.txt",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.py": {
      "includeFiles": "{backend/**,demo/demo.db,frontend/dist/**}",
      "maxDuration": 20
    }
  }
}
